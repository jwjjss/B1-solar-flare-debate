# 任务定义声明（B-1 耀斑触发前兆因果链发现）

> 本声明参照组委会《赛道一-方向2》任务定义模板格式编写，**按 B-1 假设生成任务性质改写**。
> 原始模板 `01_任务定义声明模板.md` 中的"预报时间窗""正类定义""二分类阈值"等预测任务字段，
> 在本声明中对应改写为"科学问题描述""输出结构""辩论终止条件"等假设生成任务字段。

---

队名：____________________  
作品名称：**B-1 耀斑触发前兆因果链发现 · 多智能体科学辩论系统**  
题目编号：XH-202619  
选题：赛道一 · 方向2 · 选题B · B-1 耀斑触发前兆因果链发现  
联系人 / 邮箱 / 电话：____________________  
提交日期：2026-09-04  
路线（可多选）：□ 训练模型（SFT 微调，加分项） □ **智能体（主路线）**

---

## 1. 输入

### 1.1 使用模态

- □ 文献文本（arXiv 论文摘要与全文）
- □ PNG 磁场图像（JW-FD 公开数据集 PNG Th1000）
- □ CSV 特征数据（JW-FD 公开示例当前读取 29 个数值特征 + 耀斑等级标签）
- □ FITS 科学图像（JW-FD 原始磁场观测；当前版本未接入主推理链路）
- □ 其它：辩论日志（debate_log.jsonl，多轮结构化辩论过程）

### 1.2 单样本定义

一次推理输入为一个**科学问题描述**（如"太阳耀斑触发前兆因果链发现"），系统从文献库和观测数据中
自动检索、抽取、辩论，最终输出可验证的因果链与科学假设。

### 1.3 预处理摘要

| 模态 | 预处理方式 |
|------|-----------|
| 文献（arXiv） | 标题+摘要解析，关键词检索 → Qwen 提取前兆候选因子，每个因子标注 source（arXiv ID） |
| JW-FD CSV | 特征列加载，与候选因子做统计关联分析（Pearson/Spearman 相关性 + p 值检验），不做模型训练 |
| JW-FD 图像 | 预留 Qwen-VL 视觉特征接口（当前配置 `use_vl: false`，不计入本版结果） |
| 辩论日志 | 每轮角色发言记录，含 round、speaker、role、content 字段，用于因果图谱构建 |

---

## 2. 科学假设生成任务

### 2.1 闭环链路（核心任务定义）

本系统实现完整 AI Scientist 科研闭环，四步缺一不可：

```
① 生成候选假设  →  ② 构建证据链  →  ③ 查找反例  →  ④ 提出验证计划
       ↑                                                              │
       └──────────── 迭代修正（反例成立时回到①） ◄─────────────────────┘
```

| 环节 | 系统动作 | 产出 |
|------|---------|------|
| ① 生成候选假设 | 文献 Agent 从 arXiv 论文中抽取耀斑前兆候选因子 | `literature_facts.json` |
| ② 构建证据链 | 数据证据 Agent 用 JW-FD 做统计关联验证每个因子的支撑/反驳证据 | `evidence_report.json` |
| ③ 查找反例 | 三角色辩论（物理学家/质疑者/方法论审查）多轮互怼，质疑者主动寻找逻辑漏洞与反例 | `transcript.json` + `debate_log.jsonl` |
| ④ 提出验证计划 | 假设生成 Agent 输出 11 字段《科学假设与研究计划》，含下一步观测/实验方案 | `hypothesis_report.json` |

### 2.2 输出结构（11 字段标准化报告）

系统最终输出符合赛题规范的《科学假设与研究计划》，包含以下字段：

| # | 字段 | 说明 |
|---|------|------|
| 1 | `problem_statement` | 待研究问题：当前耀斑前兆识别的具体局限性 |
| 2 | `rationale` | 解决思路：基于逻辑推理的创新点与推导链条 |
| 3 | `technical_details` | 技术手段：验证假设所需的技术栈 |
| 4 | `datasets` | 使用的数据集：JW-FD（公开示例由程序自动读取实际样本数和特征列） |
| 5 | `source` | 假设推演依据的历史数据：arXiv 文献抽取 + JW-FD 统计关联 |
| 6 | `target` | 验证实验所需的拟采集数据特征 |
| 7 | `paper_title` | 符合学术出版规范的标题 |
| 8 | `abstract` | 包含背景、方法、预期结果的完整摘要 |
| 9 | `methods` | 具体的实施步骤（模型架构或实验流程） |
| 10 | `experiments` | 实验设计，含基线对比及评估指标 |
| 11 | `references` | 系统引用的真实文献列表（**严禁虚构**，均以 arXiv ID 或 DOI 标识） |

此外，系统同时输出 `causal_graph.json`（因果图谱，含节点、边、关系类型、置信度），
供前端可视化展示辩论过程与因果链。

### 2.3 辩论终止条件（替代原模板的"阈值策略"）

| 条件 | 说明 |
|------|------|
| 默认最大轮次 | `config.yaml` 中 `debate.max_rounds: 8`，每角色发言 `debate.rounds: 2` 轮 |
| 提前终止 | 质疑者连续 2 轮无新质疑点，辩论提前终止 |
| 置信度阈值 | `config.yaml` 中 `debate.confidence_threshold: 0.5`，低于阈值的因果边标记为待验证 |
| 随机性控制 | LLM temperature 固定为 0；当前客户端未统一暴露 seed，复现时固定模型、提示词、轮次和输入数据版本 |

---

## 3. 数据使用

- ✅ 使用竞赛公开数据集：**JW-FD**（含 PNG/FITS 与特征 CSV；本版统计流程使用 CSV）
- ✅ 使用公开文献库：**arXiv**（太阳耀斑相关论文，通过 arxiv API 检索）
- ✅ 使用公开数据集：**JW-SSD**（太阳黑子表征数据，可选补充证据来源）
- ☐ SFT 微调（加分项）：当前版本未启用，`config.yaml` 中 `use_sft_model: false`

### 外部数据/预训练清单

| 名称 | 用途 | 获取方式 | 许可 |
|------|------|----------|------|
| arXiv API | 文献检索 | arxiv.org（公开 API） | 公开 |
| JW-FD 数据集 | 统计证据验证 | 组委会公开数据包 | 竞赛专用 |
| JW-SSD 数据集 | 补充证据（可选） | 组委会公开数据包 | 竞赛专用 |

### 合规声明

- ✅ 文献来源全部真实可查（arXiv ID / DOI）
- ✅ 统计结论基于 JW-FD 公开数据，p < 0.05 为显著水平
- ✅ 系统输出中的参考文献严禁虚构

---

## 4. 复现入口（与代码包一致）

### 4.1 一键推理命令

```bash
# 安装依赖
pip install -r requirements.txt

# 标准批量推理入口（mock 模式，无需 API Key）
python infer.py --out outputs/reproduce

# 真实模式（配置 .env 中的 DASHSCOPE_API_KEY 或 BAILIAN_API_KEY 后）
python infer.py --real --data_root .. --out outputs/real
```

运行完成后 `outputs/` 目录产出三个文件：

| 文件 | 内容 |
|------|------|
| `transcript.json` | 三角色辩论全过程（物理学家 / 质疑者 / 方法论审查） |
| `causal_graph.json` | 因果图谱（节点 + 边 + 关系 + 置信度） |
| `hypothesis_report.json` | 11 字段标准科学假设报告 |

### 4.2 批量推理入口（终评用）

```bash
export DASHSCOPE_API_KEY=***              # 组委会提供或使用队伍受限密钥
python infer.py \
  --data_root /path/to/data_root \
  --real \
  --out /path/to/out_dir
```

`infer.py` 对指定科学问题运行全链路，输出四个中间契约和最终假设报告 JSON；
推理阶段不读取 test 标签。

### 4.3 环境配置

| 项 | 说明 |
|---|------|
| Python 版本 | 3.11+ |
| 依赖文件 | `requirements.txt`（钉死版本） |
| 配置模板 | `config.example.yaml`（由 `config.yaml` 脱敏生成） |
| 密钥管理 | API Key 通过环境变量 `DASHSCOPE_API_KEY` 注入，不硬编码 |
| 框架依赖 | AutoGen（多角色辩论）、Qwen 系列（百炼平台）、pandas/scipy（统计） |
| 硬件需求 | CPU 可运行（mock 模式）；真实模式需 API 网络访问 |
| 单次推理预计耗时 | 约 2-5 分钟（mock 模式 < 10 秒） |

### 4.4 随机性控制

- LLM `temperature` 固定为 0（各 Agent 调用处配置）
- 当前客户端未统一暴露 seed；复现时固定模型、提示词、轮次和输入数据版本
- 辩论轮次固定（`debate.rounds: 2`）
- 确保同一输入产生一致的输出（评审可复现性要求）

---

## 5. 诚信声明

本队确认：

1. 接受组委会复现抽查：可在组委会环境下按 README 命令一键运行并产出标准输出。
2. 所有系统输出中引用的**参考文献必须真实存在**，以 arXiv ID 或 DOI 标识，严禁虚构。
3. 所有统计结论基于 JW-FD 公开数据集，不虚构数据或结论。
4. 基座模型使用阿里云百炼平台 Qwen 系列，符合赛题技术约束。
5. 提交物中不包含任何真实 API Key、网盘 cookie、SSH 私钥等敏感信息。

队长签字（或电子确认）：____________________　日期：____________________
